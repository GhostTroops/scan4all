id: x-host  

info:
  name: x-host  
  author: Kabilan S
  severity: low

requests:
  - method: GET
    # Example of sending some headers to the servers
    headers:
      
      X-Host: "evil.com"
    path:
      - "{{BaseURL}}/"
    matchers:
      - type: word
        words:
          - "evil"
