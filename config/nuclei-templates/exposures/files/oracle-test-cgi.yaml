id: oracle-testnmanp-cgi

info:
  name: Oracle Application Server testnmanp-cgi Page
  author: DhiyaneshDk
  severity: info
  metadata:
    verified: true
    shodan-query: title:"Oracle Application Server"
  tags: oracle,exposure

requests:
  - method: GET
    path:
      - "{{BaseURL}}/cgi-bin/testnmanp-cgi"

    matchers-condition: and
    matchers:
      - type: word
        part: body
        words:
          - "SERVER_SOFTWARE ="

      - type: word
        part: header
        words:
          - "text/plain"

      - type: status
        status:
          - 200
